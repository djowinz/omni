// Barrel export for @omni/shared-types.
//
// The generated/ directory is populated by `cargo test -p shared`.
// Re-export types here with curated groupings. Consumers import from
// "@omni/shared-types", never from "./generated" directly.
//
// This file is intentionally empty pending Phase 7's regeneration.
// After Phase 7 runs, update this file to re-export the generated types.

export {};
