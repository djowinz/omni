[package]
name = "omni-guard-trait"
version = "0.1.0"
edition = "2021"
publish = false

[features]
default = []
serde = ["dep:serde"]

[dependencies]
thiserror = "1"
sha2 = "0.10"
serde = { version = "1", features = ["derive"], optional = true }

[dev-dependencies]
serde_json = "1"
