[package]
name = "omni-contracts-smoke"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
omni-sanitize = { path = "../omni-sanitize" }
omni-bundle   = { path = "../omni-bundle" }
omni-guard-trait = { path = "../omni-guard-trait" }
omni-identity = { path = "../omni-identity" }
